application:
  environment:
    name: dev
  appConfigs:
    config-batch: ${application.config-server-base-url}/config-batch/dev
    task-server: ${application.config-server-base-url}/task-server/dev
    refresh-batch: ${application.config-server-base-url}/refresh-batch/dev
